<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Manual hand-signed upload for the DELIVERY NOTE portal rows.
         The delivery rows live inside the SO portal page (sale_stock
         inheritance), so the upload button + modal sit INSIDE the
         per-picking actions div (picking in scope there). Posts to
         /my/picking/<id>/confirm_viva. -->
    <template id="sale_order_portal_viva_manual_upload_delivery"
              name="VivaFarm Sale Order Portal Manual Delivery Upload"
              inherit_id="sale_stock.sale_order_portal_content_inherit_sale_stock">
        <xpath expr="//div[@name='viva_delivery_actions']" position="inside">
            <a
                role="button"
                class="btn btn-sm btn-light"
                data-bs-toggle="modal"
                t-att-data-bs-target="'#modalmanual_upload_delivery_%s' % picking.id"
                href="#"
                t-att-id="'manual_upload_delivery_button_%s' % picking.id"
            >
                <i class="fa fa-upload me-1"/>Upload hand-signed copy
            </a>
        </xpath>
        <xpath expr="//div[@name='viva_delivery_actions']" position="after">
            <div role="dialog" class="modal fade"
                 t-att-id="'modalmanual_upload_delivery_%s' % picking.id"
                 name="sale_order_modal_manual_upload_delivery">
                <div class="modal-dialog">
                    <form method="post"
                          t-att-action="'/my/picking/%s/confirm_viva' % picking.id"
                          enctype="multipart/form-data"
                          class="modal-content">
                        <input type="hidden" name="access_token" t-att-value="sale_order.access_token"/>
                        <header class="modal-header">
                            <h4 class="modal-title">Upload hand-signed delivery note</h4>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"/>
                        </header>
                        <main class="modal-body">
                            <p>
                                Print the delivery note, sign it by hand, scan or photograph it,
                                then upload the file (PDF, PNG or JPG, max 10&#160;MB).
                            </p>
                            <div class="mb-3">
                                <label class="form-label">File</label>
                                <input type="file" name="upload" accept="application/pdf,image/png,image/jpeg" class="form-control" required="required"/>
                            </div>
                            <div class="mb-3">
                                <label class="form-label">Your full name</label>
                                <input type="text" name="uploader_name" class="form-control" required="required"
                                       t-att-value="sale_order.partner_id.name or ''"/>
                            </div>
                            <div class="form-check">
                                <input type="checkbox" name="confirm" class="form-check-input" required="required" value="1"/>
                                <label class="form-check-label">
                                    I confirm this is the signed delivery note for this order.
                                </label>
                            </div>
                            <p class="text-muted small mt-2">
                                The uploaded file is stored unaltered and can be verified at any time.
                                This does not constitute a digital signature.
                            </p>
                        </main>
                        <footer class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                            <button type="submit" class="btn btn-primary"><i class="fa fa-upload me-1"/>Upload</button>
                        </footer>
                    </form>
                </div>
            </div>
        </xpath>
    </template>
</odoo>
