/* Omise payment form — placeholder for Slice 5 (QR display + expiry). */
odoo.define('vivafarm_payment_omise.omise_payment_form', function (require) {
    'use strict';

    return {
        init: function () {},
    };
});
