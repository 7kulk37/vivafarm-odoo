<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="inline_form">
        <div name="o_omise_card_form" class="o_omise_card_form">
            <div class="form-group mb-2">
                <label for="o_omise_card_name">Cardholder name</label>
                <input type="text" id="o_omise_card_name" name="card[name]"
                       class="form-control" placeholder="Name on card" autocomplete="cc-name"/>
            </div>
            <div class="form-group mb-2">
                <label for="o_omise_card_number">Card number</label>
                <input type="text" id="o_omise_card_number" name="card[number]"
                       class="form-control" placeholder="4242 4242 4242 4242"
                       autocomplete="cc-number" inputmode="numeric"/>
            </div>
            <div class="row">
                <div class="col-6 form-group mb-2">
                    <label for="o_omise_card_exp">Expiry</label>
                    <input type="text" id="o_omise_card_exp" name="card[expiration_month]"
                           class="form-control" placeholder="MM" autocomplete="cc-exp-month"
                           inputmode="numeric" maxlength="2"/>
                </div>
                <div class="col-6 form-group mb-2">
                    <label for="o_omise_card_cvc">CVC</label>
                    <input type="text" id="o_omise_card_cvc" name="card[security_code]"
                           class="form-control" placeholder="123" autocomplete="cc-csc"
                           inputmode="numeric" maxlength="4"/>
                </div>
            </div>
        </div>
    </template>

    <template id="omise_state_header" inherit_id="payment.state_header">

        <xpath expr="//div[@name='o_payment_status_alert']" position="inside">
            <t t-if="tx.provider_id.sudo().code == 'omise' and tx.state == 'pending' and tx.omise_qr_url">
                <div class="o_qr_code_card card bg-info flex-shrink-0 mt-3">
                    <div class="card-body d-flex flex-column align-items-center justify-content-center pb-3">
                        <img class="mb-2 border border-dark rounded"
                             t-att-src="tx.omise_qr_url"
                             alt="PromptPay QR code"
                             style="max-width: 240px;"
                        />
                        <small class="text-center text-wrap lh-sm">
                            Scan this QR code with your banking app to complete the payment
                        </small>
                    </div>
                </div>
            </t>
        </xpath>

    </template>

</odoo>
